API endpoint
    https://kchsyh6ha4.execute-api.ap-northeast-2.amazonaws.com/
    
AddMyOrder
    AccessToken: GetAccessTokenFromAPIKey에서 얻는 토큰
    AssetPair: ETH/BTC 등
    Action: Buy, Sell
    UnitPrice: 단가
    Amount: 수량
    
    위의 조건으로 오더를 낸다. 성공하면 아래와 같은 JSON이 온다.
    
    {'OrderID':'', 'AssetPair':'', 'Timestamp':'', 'Action':'', 'UnitPrice':'', 'Amount':''}
    
    이 때 받은 OrderID를 보관해야 나중에 이 오더를 지울 수 있다. 다만, 지금 받은 OrderID가 항상 존재한다고 믿으면 안 된다. 체결 등의 이유로 이 오더가 사라질 수 있다.
    그래서 아래에 있는 GetMyOrders를 불러서 현재의 오더들을 확인해야 한다.
    
GetAccessTokenFromAPIKey
    APIKey: 주어진 계정의 api key
    
    이 APIKey 주인의 AccessToken을 얻는다. 이 토큰은 한 시간 후에 사라지므로 그 후에 다시 얻어야 한다. 다른 api를 부를 때 항상 필요한 토큰이다.
    회원이 손으로 로긴해도 이 토큰을 얻지만 api로 얻는 것과 동일하며 이 둘은 겹치지 않고 한 회원에 대해서 유일하다.
    
GetLastPrice
    AccessToken: GetAccessTokenFromAPIKey에서 얻는 토큰
    AssetPair: ETH/BTC 등
    
    AccessToken 회원의 AssetPair 종목에서의 거래 중 마지막 가격을 가져온다.
    
RemoveMyOrder
    AccessToken
    OrderID
    
    AccessToken 회원의 Timestamp의 주문이 지워진다.
